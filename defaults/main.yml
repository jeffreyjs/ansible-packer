---
packer_arch: "{% if ansible_architecture == 'x86_64' %}amd64{% else %}{{ ansible_architecture }}{% endif %}"

packer_bin_path: /usr/local/bin

packer_repo: hashicorp/packer

packer_state: install

# packer_version: "1.0.0"
